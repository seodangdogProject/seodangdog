import styled from "./DashboardContainer.module.css";
import classNames from "classnames/bind";
export default function DashboardContainer() {
  const cd = classNames.bind(styled);
  return <>대쉬보드다</>;
}
